// Geometric primitives shared across the canvas-based editors (crop, template,
// caption timeline, …).

export interface Point {
  x: number
  y: number
}

export interface Rect {
  x: number
  y: number
  width: number
  height: number
}
